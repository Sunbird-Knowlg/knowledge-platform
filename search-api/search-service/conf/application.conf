# This is the main configuration file for the application.
# https://www.playframework.com/documentation/latest/ConfigFile
# ~~~~~
# Play uses HOCON as its configuration file format.  HOCON has a number
# of advantages over other config formats, but there are two things that
# can be used when modifying settings.
#
# You can include other configuration files in this main application.conf file:
#include "extra-config.conf"
#
# You can declare variables and substitute for them:
#mykey = ${some.value}
#
# And if an environment variable exists when there is no other substitution, then
# HOCON will fall back to substituting environment variable:
#mykey = ${JAVA_HOME}

## Akka
# https://www.playframework.com/documentation/latest/ScalaAkka#Configuration
# https://www.playframework.com/documentation/latest/JavaAkka#Configuration
# ~~~~~
# Play uses Akka internally and exposes Akka Streams and actors in Websockets and
# other streaming HTTP responses.
akka {
  # "pekko.log-config-on-start" is extraordinarly useful because it log the complete
  # configuration at INFO level, including defaults and overrides, so it s worth
  # putting at the very top.
  #
  # Put the following in your conf/logback.xml file:
  #
  # <logger name="pekko.actor" level="INFO" />
  #
  # And then uncomment this line to debug the configuration.
  #
  #log-config-on-start = true
}

## Secret key
# http://www.playframework.com/documentation/latest/ApplicationSecret
# ~~~~~
# The secret key is used to sign Play's session cookie.
# This must be changed for production, but we don't recommend you change it in this file.
play.http.secret.key = a-long-secret-to-calm-the-rage-of-the-entropy-gods

## Modules
# https://www.playframework.com/documentation/latest/Modules
# ~~~~~
# Control which modules are loaded when Play starts.  Note that modules are
# the replacement for "GlobalSettings", which are deprecated in 2.5.x.
# Please see https://www.playframework.com/documentation/latest/GlobalSettings
# for more information.
#
# You can also extend Play functionality by using one of the publically available
# Play modules: https://playframework.com/documentation/latest/ModuleDirectory
play.modules {
  # By default, Play will load any class called Module that is defined
  # in the root package (the "app" directory), or you can define them
  # explicitly below.
  # If there are any built-in modules that you want to enable, you can list them here.
  #enabled += my.application.Module

  # If there are any built-in modules that you want to disable, you can list them here.
  #disabled += ""
  enabled += modules.SearchModule
}

## IDE
# https://www.playframework.com/documentation/latest/IDE
# ~~~~~
# Depending on your IDE, you can add a hyperlink for errors that will jump you
# directly to the code location in the IDE in dev mode. The following line makes
# use of the IntelliJ IDEA REST interface:
#play.editor="http://localhost:63342/api/file/?file=%s&line=%s"

## Internationalisation
# https://www.playframework.com/documentation/latest/JavaI18N
# https://www.playframework.com/documentation/latest/ScalaI18N
# ~~~~~
# Play comes with its own i18n settings, which allow the user's preferred language
# to map through to internal messages, or allow the language to be stored in a cookie.
play.i18n {
  # The application languages
  langs = [ "en" ]

  # Whether the language cookie should be secure or not
  #langCookieSecure = true

  # Whether the HTTP only attribute of the cookie should be set to true
  #langCookieHttpOnly = true
}

## Play HTTP settings
# ~~~~~
play.http {
  ## Router
  # https://www.playframework.com/documentation/latest/JavaRouting
  # https://www.playframework.com/documentation/latest/ScalaRouting
  # ~~~~~
  # Define the Router object to use for this application.
  # This router will be looked up first when the application is starting up,
  # so make sure this is the entry point.
  # Furthermore, it's assumed your route file is named properly.
  # So for an application router like `my.application.Router`,
  # you may need to define a router file `conf/my.application.routes`.
  # Default to Routes in the root package (aka "apps" folder) (and conf/routes)
  #router = my.application.Router

  ## Action Creator
  # https://www.playframework.com/documentation/latest/JavaActionCreator
  # ~~~~~
  #actionCreator = null

  ## ErrorHandler
  # https://www.playframework.com/documentation/latest/JavaRouting
  # https://www.playframework.com/documentation/latest/ScalaRouting
  # ~~~~~
  # If null, will attempt to load a class called ErrorHandler in the root package,
  #errorHandler = null

  ## Session & Flash
  # https://www.playframework.com/documentation/latest/JavaSessionFlash
  # https://www.playframework.com/documentation/latest/ScalaSessionFlash
  # ~~~~~
  session {
    # Sets the cookie to be sent only over HTTPS.
    #secure = true

    # Sets the cookie to be accessed only by the server.
    #httpOnly = true

    # Sets the max-age field of the cookie to 5 minutes.
    # NOTE: this only sets when the browser will discard the cookie. Play will consider any
    # cookie value with a valid signature to be a valid session forever. To implement a server side session timeout,
    # you need to put a timestamp in the session and check it at regular intervals to possibly expire it.
    #maxAge = 300

    # Sets the domain on the session cookie.
    #domain = "example.com"
  }

  flash {
    # Sets the cookie to be sent only over HTTPS.
    #secure = true

    # Sets the cookie to be accessed only by the server.
    #httpOnly = true
  }
}

play.http.parser.maxDiskBuffer = 10MB
parsers.anyContent.maxLength = 10MB

play.server.provider = play.core.server.PekkoHttpServerProvider

## Netty Provider
# https://www.playframework.com/documentation/latest/SettingsNetty
# ~~~~~
play.server.netty {
  # Whether the Netty wire should be logged
  log.wire = true

  # If you run Play on Linux, you can use Netty's native socket transport
  # for higher performance with less garbage.
  transport = "native"
}

## WS (HTTP Client)
# https://www.playframework.com/documentation/latest/ScalaWS#Configuring-WS
# ~~~~~
# The HTTP client primarily used for REST APIs.  The default client can be
# configured directly, but you can also create different client instances
# with customized settings. You must enable this by adding to build.sbt:
#
# libraryDependencies += ws // or javaWs if using java
#
play.ws {
  # Sets HTTP requests not to follow 302 requests
  #followRedirects = false

  # Sets the maximum number of open HTTP connections for the client.
  #ahc.maxConnectionsTotal = 50

  ## WS SSL
  # https://www.playframework.com/documentation/latest/WsSSL
  # ~~~~~
  ssl {
    # Configuring HTTPS with Play WS does not require programming.  You can
    # set up both trustManager and keyManager for mutual authentication, and
    # turn on JSSE debugging in development with a reload.
    #debug.handshake = true
    #trustManager = {
    #  stores = [
    #    { type = "JKS", path = "exampletrust.jks" }
    #  ]
    #}
  }
}

## Cache
# https://www.playframework.com/documentation/latest/JavaCache
# https://www.playframework.com/documentation/latest/ScalaCache
# ~~~~~
# Play comes with an integrated cache API that can reduce the operational
# overhead of repeated requests. You must enable this by adding to build.sbt:
#
# libraryDependencies += cache
#
play.cache {
  # If you want to bind several caches, you can bind the individually
  #bindCaches = ["db-cache", "user-cache", "session-cache"]
}

## Filter Configuration
# https://www.playframework.com/documentation/latest/Filters
# ~~~~~
# There are a number of built-in filters that can be enabled and configured
# to give Play greater security.
#
play.filters {

  # Enabled filters are run automatically against Play.
  # CSRFFilter, AllowedHostFilters, and SecurityHeadersFilters are enabled by default.
  enabled = [play.filters.headers.SecurityHeadersFilter, filters.AccessLogFilter]

  # Disabled filters remove elements from the enabled list.
  # disabled += filters.CSRFFilter


  ## CORS filter configuration
  # https://www.playframework.com/documentation/latest/CorsFilter
  # ~~~~~
  # CORS is a protocol that allows web applications to make requests from the browser
  # across different domains.
  # NOTE: You MUST apply the CORS configuration before the CSRF filter, as CSRF has
  # dependencies on CORS settings.
  cors {
    # Filter paths by a whitelist of path prefixes
    #pathPrefixes = ["/some/path", ...]

    # The allowed origins. If null, all origins are allowed.
    #allowedOrigins = ["http://www.example.com"]

    # The allowed HTTP methods. If null, all methods are allowed
    #allowedHttpMethods = ["GET", "POST"]
  }

  ## Security headers filter configuration
  # https://www.playframework.com/documentation/latest/SecurityHeaders
  # ~~~~~
  # Defines security headers that prevent XSS attacks.
  # If enabled, then all options are set to the below configuration by default:
  headers {
    # Values: "DENY" (block all framing), "SAMEORIGIN" (allow same-origin framing), null (disable header)
    frameOptions = "DENY"
    # Values: "1; mode=block" (enable legacy XSS auditor), null (disable — recommended, auditor is exploitable)
    xssProtection = null
    # Values: "nosniff" (prevent MIME-type sniffing), null (disable header)
    contentTypeOptions = "nosniff"
    # Values: "none", "master-only", "by-content-type", "all", null (disable — not needed for JSON APIs)
    permittedCrossDomainPolicies = null
    # Values: CSP directive string e.g. "default-src 'self'", null (disable — not meaningful for JSON APIs)
    contentSecurityPolicy = null
  }

  ## Allowed hosts filter configuration
  # https://www.playframework.com/documentation/latest/AllowedHostsFilter
  # ~~~~~
  # Play provides a filter that lets you configure which hosts can access your application.
  # This is useful to prevent cache poisoning attacks.
  hosts {
    # Allow requests to example.com, its subdomains, and localhost:9000.
    #allowed = [".example.com", "localhost:9000"]
  }
}

play.http.parser.maxMemoryBuffer = 50MB
pekko.http.parsing.max-content-length = 50MB

schema.base_path="../../schemas/"


content.relatedBoards.properties=["channel","board","subject","medium"]


# Path for elasticsearch.properties file
search.config.path=/home/learning/platform/search

# Cache-Manager Configuration
cache.type="redis"

search.es_conn_info="localhost:9200"
search.fields.query=["name^100","title^100","lemma^100","code^100","domain","subject","description^10","keywords^100","ageGroup^10","filter^10","theme^10","genre^10","objects^25","contentType^100","language^200","teachingMode^25","skills^10","learningObjective^10","curriculum^100","gradeLevel^100","developer^100","attributions^10","identifier^100","IL_UNIQUE_ID^100","owner^50","board^100","relatedBoards^100","creator^100", "dialcodes^100","text","words","releaseNotes"]
search.fields.date=["lastUpdatedOn","createdOn","versionDate","lastSubmittedOn","lastPublishedOn"]
search.fields.mode_collection=["identifier","name","objectType","contentType","mimeType","size","childNodes","board","subject","medium","gradeLevel","appIcon", "origin", "originData"]
search.batch.size=500
search.connection.timeout=30

# Whitelists for SearchInputValidator (INJ-VULN-01..08). Override defaults; if absent, hardcoded defaults used.
search.allowed.facet.fields=["objectType","contentType","status","mimeType","primaryCategory","channel","framework","board","medium","gradeLevel","subject","resourceType","audience","license","language","domain","targetFWIds","se_boards","se_mediums","se_gradeLevels","se_subjects","visibility","compatibilityLevel","mediaType","origin","createdBy"]
search.allowed.sort.fields=["name","lastUpdatedOn","createdOn","pkgVersion","identifier","objectType","contentType","status","mimeType","primaryCategory","lastPublishedOn","lastSubmittedOn","rating","me_averageRating"]
search.allowed.exists.fields=["name","description","status","mimeType","primaryCategory","objectType","contentType","channel","framework","board","medium","gradeLevel","subject","artifactUrl","downloadUrl","variants","pkgVersion","audience","license","language","lastPublishedOn","identifier","createdOn","lastUpdatedOn","resourceType","appIcon","posterImage","visibility","compatibilityLevel","mediaType","origin","rating"]
# Max counts — guard against payload bloat / DoS
search.max.facets=20
search.max.sort.fields=5
search.max.exists.fields=10

platform-api-url="http://localhost:8080/learning-service"
language.map={"Hindi":"hi", "English":"en", "Telugu":"te", "Kannada":"ka", "Tamil":"ta", "Assamese":"as", "Bengali":"bn", "Bodo":"bo", "Gujarati":"gu", "Konkani":"ko", "Malayalam":"ml", "Marathi":"mr", "Nepali":"ne", "Odia":"or", "Punjabi":"pj", "Sanskrit":"sk"}
#Top N Config for Search Telemetry
telemetry.search.topn=5
telemetry_env=dev
installation.id=ekstep

# Configuration for default channel ID
channel.default="in.ekstep"
ekstepPlatformApiUserId="search-service"


content.tagging.property=["subject","medium"]
search.payload.log_enable=true

compositesearch.index.name="compositesearch"

audit.index.name="kp_audit_log"

# Semantic search configuration. Disabled by default; turn on per env.
# Vectors at index time are produced by content-embedding-job — the values
# under embedding/quantization here must match the job's config.
semantic_search {
  enabled = true

  embedding_service = "openai"

  openai {
    api_key = ""
    api_key = ${?OPENAI_API_KEY}
    model = "text-embedding-3-small"
    dimensions = 1536
    timeout = 5
    azure_endpoint = ""
    azure_endpoint = ${?AZURE_OPENAI_ENDPOINT}
    azure_deployment = ""
    azure_deployment = ${?AZURE_OPENAI_DEPLOYMENT}
    azure_api_version = "2023-05-15"
  }

  e5 {
    host = "localhost"
    port = 80
    dimensions = 768
    timeout = 5
  }

  quantization_strategy = "int8"

  vector_field = "chunks.embedding"
  vector_path  = "chunks"

  default_k = 50
  max_k     = 500
  min_score = 0.75

  schema_versions = ["1.0"]

  rrf_k = 60

  embedding_pool_size = 8

  embedding_cache {
    enabled     = true
    size        = 1024
    ttl_seconds = 300
  }
}

# Cloud Storage configuration
# StorageModule (modules.StorageModule) is not enabled here because search-service
# has no cloud-storage actors. Enable it if cloud-using actors are added in the future
# (also add mimetype-manager dependency to search-service pom.xml at that point).
cloud_storage_type="azure"
# cloud_storage_region: AWS S3 only — region of the bucket. Ignored by Azure.
# Leave unset for us-east-1 (SDK default); required for buckets in any other AWS region.
#cloud_storage_region="ap-south-1"
# ACCESS_KEY for local dev; OIDC for production K8s (Azure Workload Identity)
cloud_storage_auth_type="ACCESS_KEY"
cloud_storage_key=""
cloud_storage_secret=""
cloud_storage_container=""
# Redis Configuration
# Redis is not currently used by search-api. cache.type="redis" above is a legacy dead key.
redis.enable = false

# Enrich API Configuration
enrich.max_identifiers=100

# Kafka Configuration (used by EnrichActor to emit BE_JOB_REQUEST events)
kafka.urls="localhost:9092"
kafka.publish.request.topic="sunbirddev.knowlg.publish.job.request"

# HTML Sanitizer Settings
html.sanitizer.max.depth=25
